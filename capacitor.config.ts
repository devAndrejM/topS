import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clothingsearch.app',
  appName: 'ClothingSearch.Frontend',
  webDir: 'www'
};

export default config;
